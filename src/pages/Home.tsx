import React from 'react';

// Placeholder — the living-dashboard layout is composed in Phase 5.
const Home: React.FC = () => (
  <main className="min-h-screen bg-base text-ink flex items-center justify-center">
    <h1 className="font-serif text-2xl">Sarthak Sethi</h1>
  </main>
);

export default Home;
