# GitHub token for the live "GitHub Pulse" (/api/github).
# Needs only public read scope (classic: public_repo / read:user, or a fine-grained
# token with read-only public access). Set the same value in the Vercel project settings.
# Without it, /api/github serves the committed fallback snapshot (live:false).
GITHUB_TOKEN=
